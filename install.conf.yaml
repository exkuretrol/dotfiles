- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
#     ~/.vim: vim
#     ~/.vimrc: vimrc

- create:
    - ~/Workspace

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]

