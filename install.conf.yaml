- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.config/bat:
        create: true
        path: config/bat

- create:
    - ~/Workspace

- shell:
    - 
      command: ./setup_homebrew.zsh
      stdout: true
      stdin: true
      stderr: true
