- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/Workspace

- shell:
#   - [git submodule update --init --recursive, Installing submodules]
    - 
      command: ./test.zsh
      stdout: true
      stdin: true
      stderr: true
